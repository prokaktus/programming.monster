name: Python lint

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Hello message
        run: |
          echo 'Hello'
      - name: Test docker action
        uses: actions/build-container
        with:
          who-to-greet: 'Jonny'
